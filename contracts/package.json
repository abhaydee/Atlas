{
  "name": "oracle-synthetic-contracts",
  "version": "1.0.0",
  "description": "Solidity contracts for Oracle-Backed Synthetic Asset Protocol",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network kiteTestnet",
    "deploy:local": "hardhat run scripts/deploy.ts --network hardhat",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}