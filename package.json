{
  "name": "kite-ai-trade-agent",
  "version": "1.0.0",
  "description": "Atlas — Truly permissionless markets. Autonomous AI agent and x402 protocol.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "agent": "tsx src/index.ts --auto",
    "test:payment": "tsx src/test-payment.ts",
    "pool:server": "tsx src/pool/pool-server.ts",
    "pool:agent": "tsx src/pool/pool-agent.ts",
    "pool:agent:demo": "tsx src/pool/pool-agent.ts --demo",
    "protocol:compile": "cd contracts && npm install && npm run compile",
    "protocol:deploy": "cd contracts && npm run deploy",
    "protocol:backend": "cd backend && npm install && npm run dev",
    "protocol:frontend": "cd frontend && npm install && npm run dev"
  },
  "keywords": [
    "kite",
    "ai-agent",
    "x402",
    "mcp",
    "blockchain",
    "payments"
  ],
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "chalk": "^5.6.2",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "openai": "^6.22.0",
    "ora": "^9.3.0",
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
