# ── Kite Testnet ──────────────────────────────────────────────────────────────
RPC_URL=https://rpc-testnet.gokite.ai
CHAIN_ID=2368

# ── Agent Wallet (CRITICAL — keep secret, never commit) ────────────────────────
# This private key is the agent's autonomous identity.
# It signs x402 payments and deploys contracts — no MetaMask needed.
PRIVATE_KEY=your_private_key_here

# ── Contracts ─────────────────────────────────────────────────────────────────
# Testnet USDT on Kite — 6 decimals
USDC_ADDRESS=0x0fF5393387ad2f9f691FD6Fd28e07E3969e27e63

# Optional: real Switchboard feed. Leave empty to deploy OracleAggregator.
SWITCHBOARD_FEED_ADDRESS=

# ── x402 Payment Config ───────────────────────────────────────────────────────
# Wallet address that receives create-market payment fees
PAYEE_ADDRESS=0x1a5de860035E2E388140345a0F15897A19A92DB8

# Pieverse facilitator for Kite testnet settlement
FACILITATOR_URL=https://facilitator.pieverse.io

# Set to "true" to skip agent payment (dev / demo testing only)
# X402_DISABLE=true

# ── Agent Security Controls ───────────────────────────────────────────────────
# Max USDT the agent can spend in a single create-market request
AGENT_SPEND_CAP_PER_REQUEST=50

# Max USDT the agent can spend in a rolling 24-hour window
AGENT_DAILY_SPEND_CAP=500

# Rate limiting: max requests per window
RATE_LIMIT_MAX_REQUESTS=10
RATE_LIMIT_WINDOW_MS=60000

# Set to "true" to immediately disable all agent actions (emergency kill-switch)
AGENT_REVOKED=false

# ── OpenRouter (AI Research) ──────────────────────────────────────────────────
OPENROUTER_API_KEY=
OPENROUTER_MODEL=mistralai/mistral-7b-instruct:free

# ── Server ────────────────────────────────────────────────────────────────────
PORT=3000
BACKEND_URL=http://localhost:3000

# ── Oracle Scheduler ─────────────────────────────────────────────────────────
# How often (ms) to auto-refresh oracle price from research URLs (0 = disabled)
ORACLE_INTERVAL_MS=300000

# ── Pool Seed (optional) ─────────────────────────────────────────────────────
# Integer USDT to seed the AMM pool at deployment (0 = deploy empty)
POOL_SEED_USDC=0
